"""GitHub integration API routes (placeholder).

No endpoints yet. GitHub authorization (OAuth/App installation) is deferred.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/github", tags=["github"])
